# 면허로드 — 2종보통 학과시험 1,000문항 완전판

도로교통공단 공개 문제은행(2026.3.9 시행)의 **1,000문항 전체**(이미지 문항 포함)를 담은
데일리 학습 웹앱을 만든다.

## 중요: PDF는 필요 없다
원본 PDF(50MB) 파싱과 이미지 추출은 **이미 완료된 상태**로 제공된다.
`license-data.zip`(8MB)의 압축을 풀면 아래 두 가지가 들어 있고, 이것만으로 앱을 완성할 수 있다.

```
public/q/*.jpg      # 이미지 문항 카드 301장 (파일명 = 문항번호, 예: 744.jpg)
src/data/bank.json  # 1,000문항 전체 데이터
```

## 데이터 스키마 (`src/data/bank.json`)
```ts
type Q = {
  i: number;             // 공단 문제은행 번호 (1~1000, 누락 없음)
  t: 'text' | 'img';     // 렌더 방식
  a: number[];           // 정답 (1-based, 길이 1 또는 2)
  e: string;             // 해설 (320자 컷, 법령 근거 포함)
  q?: string;            // t==='text'일 때: 문제 지문
  c?: string[];          // t==='text'일 때: 보기 4개
  nc?: number;           // t==='img'일 때: 보기 버튼 개수 (4 또는 5)
};
```

> **데이터 패치 v2 적용 완료** (`PATCH.md` 참조). 최초 배포본의 두 가지 오류가 수정됐다.
> 1. 보기 마커로 `ⓛ`·`➀` 같은 변종 문자를 쓰는 8문항(#39 #262 #264 #405 #466 #509 #549 #651)이
>    파싱 실패로 이미지형에 잘못 분류돼 있었다. 순수 문장형이라 `text`로 바뀌었고, 카드 8장은 삭제했다.
> 2. 이미지형 5지선다는 **185개**다. `ⓛ②③④`인 #39·#466을 5지선다로 오인하면 187이 된다.
>
> `verify/answer-key.csv`가 정답·유형·보기 개수의 기준이다. 데이터를 건드렸다면
> `node verify/verify.mjs`로 실패 0건을 확인할 것.

### 데이터 구성 (검증 완료)
| 항목 | 수량 |
|---|---|
| 전체 문항 | 1,000 (번호 1~1000, 누락·중복 0) |
| 문장형 `t:'text'` | 707 |
| 이미지형 `t:'img'` | 293 (5지선다 185 + 4지선다 108) |
| 카드 이미지 | 293장 |
| 복수정답(2개 선택) | 282 |
| 정답 누락 | 0 |
| 해설 누락 | 3 (#418, #643, #964 — 원본 PDF에 해설이 짧거나 없음) |

### 이미지 카드에 대해
- 안전표지·사진·일러스트·동영상형 문항은 PDF의 해당 문항 영역을 그대로 크롭 렌더링한 JPG다.
- 카드 안에는 **문제 지문 + 보기(①~④ 또는 ①~⑤)까지만** 들어 있다. 정답·해설은 잘려 있다.
  (전수 검증: 카드 텍스트에 '정답'/'해설' 문자열 노출 0건, 빈 카드 0건 확인 완료)
- 폭은 전부 900px, 파일명은 `{문항번호}.jpg`. `bank.json`의 `i`값으로 바로 매칭된다.
- 따라서 이미지 문항 UI는 **카드 이미지 1장 + 그 아래 원문자 버튼 `nc`개**로 구성하면 된다.
  보기 텍스트를 따로 렌더링하지 말 것(이미 카드 안에 있음).
- 카드는 900px을 화면 폭(약 314px)으로 줄여 보여주므로 본문 글씨가 작다.
  라이트박스는 기본을 **원본 크기**로 열어 좌우로 밀며 읽게 한다.

## 만들 것 — Vite + React 앱

### 프로젝트 세팅
```bash
npm create vite@latest . -- --template react
# license-data.zip 압축 해제 후 public/q/ 와 src/data/ 를 프로젝트에 배치
```

### 디자인 (그대로 따를 것)
테마 "아스팔트 위의 하루 한 코스".
```
배경   #1F2226   카드   #2A2E34   경계선 #3A3F47
본문   #F4F5F2   흐림   #9AA0A8
액센트 #FFB800 (도로 중앙선 노랑) — 주요 버튼·강조에만
파랑   #1663D9   빨강   #E5493D   초록   #2BB673
```
- 폰트: `'IBM Plex Sans KR','Pretendard',system-ui,'Apple SD Gothic Neo',sans-serif`
- 시그니처 1 — **신호등 스트릭**: 연속 학습일에 따라 점등 (1일=빨강, 3일=노랑, 7일=초록).
  헤더 우측에 알약 모양으로 배치, 켜진 점에는 같은 색 glow.
- 시그니처 2 — **도로 중앙선 진행바**: 노란 점선(`repeating-linear-gradient`) 위를 🚗가
  진행률만큼 이동. 우측에 `현재/전체` 표기.
- 모바일 우선, 콘텐츠 최대폭 480px, 카드 radius 16px.

### 화면
1. **홈** — 오늘 날짜/오늘의 퀴즈 시작 버튼, 통계 3칸(푼 문제·정답률·오답노트),
   문제은행 정복률 바(푼 문항 / 1000), 오답노트 복습 버튼, 실기 체크리스트 버튼.
2. **퀴즈** — 랜덤 10문항. 진행바 + 문항 카드 + 보기 버튼.
   - `t:'text'` → 지문 텍스트 + 보기 4개 버튼
   - `t:'img'` → `<img src={`/q/${i}.jpg`} loading="lazy">` + ①~④ 버튼. 탭하면 확대(라이트박스)
   - `a.length===1` → 탭 즉시 채점 / `a.length===2` → 2개 토글 선택 후 "정답 확인" 버튼
   - 채점 후 정답 보기는 초록, 틀린 선택은 빨강, 아래에 해설 카드
3. **결과** — 점수(100점 환산), 60점 합격선 판정, 오답 저장 안내
4. **오답노트 복습** — `wrongIds`에서 출제. 다시 맞히면 목록에서 자동 제거
5. **실기 체크리스트** — 장내기능/도로주행 체크박스 (아래 항목 사용, localStorage 저장)

### 실기 체크리스트 항목
장내기능: 출발 전 기본조작(안전벨트·기어·사이드·방향지시등) / 경사로 정지·출발 밀림 없이 /
직각주차(T자) 사이드미러 기준점 / 신호교차로·좌우회전 깜빡이 타이밍 / 돌발 시 3초 내 급정지+비상등
도로주행: 차로변경 루틴(깜빡이→거울→어깨너머) / 교차로 황색신호 대응 / 시험 코스 사전 답사 /
제한속도 -5km 유지 / 종료 시 기어 P·사이드·시동 순서

### 저장 (localStorage, 키 `dl2-progress-v4`)
```ts
{ streak, lastDate, total, correct, wrongIds:number[], solvedIds:number[],
  practicalDone:string[],
  attempts: { [문항번호]: { n:시도횟수, w:오답횟수 } } }
```
- `attempts`는 영역별 오답률의 근거다. `wrongIds`는 '지금 틀린 상태'만 담고
  다시 맞히면 빠지므로 누적 집계가 따로 필요하다.
- v3 데이터는 첫 로드 시 자동 이관된다(푼 문항=1회 시도, 현재 오답=1회 오답).
  v3 키는 지우지 않는다.
- 스트릭: `lastDate`가 어제면 +1, 그 이전이면 1로 리셋. 같은 날 재도전은 스트릭 유지.
- 출제: `solvedIds`에 없는 문항 우선 → 1,000문항을 한 바퀴 돌게 만든다.

### 추가 기능
1. **유형 필터** — 출제 풀을 "전체 / 문장형만 / 이미지형만" 토글.
2. **실전 모의고사 모드** — 40문항·40분 타이머, 60점 합격 판정.
   배점은 문장형 2점·이미지형 3점으로 근사하되, **정확한 공식 배점이 아니므로**
   `SCORE_WEIGHTS` 상수로 분리하고 "추정 배점" 주석과 UI 문구를 남길 것.
3. **취약 영역 진단 + 영역별 학습** — 문항을 7개 영역으로 나눠 오답률을 집계하고,
   약한 영역만 골라 풀 수 있게 한다.
   - `bank.json`에 영역 정보가 **없다**. 법령명은 656/1000이 '도로교통법'이고
     조문 번호는 449문항에만 있으며 문항 번호도 주제순이 아니다. 그래서
     지문·해설 어휘로 **추정 분류**한다 — `tools/build-categories.py` →
     `src/data/categories.json`. bank.json은 건드리지 않는다.
   - 분류는 근사치이므로 화면에 "추정 분류" 문구를 남길 것.
   - 영역별 표본이 `MIN_ATTEMPTS`(5회) 미만이면 오답률을 표시하지 않는다.
   - 오늘의 퀴즈 10문항 중 `WEAK_SLOTS`(3)를 취약 영역에서 채운다.

## 완료 기준 (Definition of Done)
- [ ] 1,000문항 전부 출제 가능, 이미지 문항 293장 모두 정상 표시(404 없음)
- [ ] 복수정답 282문항이 "2개 선택 → 확인" 플로우로 동작
- [ ] 새로고침 후 스트릭·오답노트·정복률 유지
- [ ] 390px 폭에서 이미지 카드 글씨가 읽히는지 확인 (안 읽히면 라이트박스 확대 필수)
- [ ] `npm run build` 성공, 빌드 용량 리포트
- [ ] 푸터에 출처 표기: "문항 출처 — 도로교통공단 학과시험 문제은행(2026.3.9 시행)"

## PWA (홈 화면에 추가)
`vite-plugin-pwa`로 매니페스트·서비스워커를 만든다.
- 선캐시는 **앱 셸 + `bank.json`만**(약 979KB). 카드 293장(9.4MB)과 폰트 조각 92개(3.1MB)를
  통째로 넣으면 설치가 지나치게 무거워지므로, 한 번 본 것만 런타임 캐시(CacheFirst)한다.
- `registerType`은 **`prompt`**. 자동 갱신은 새 버전이 뜨는 순간 새로고침해서
  40분짜리 모의고사 답안을 날린다. 사용자가 새로고침 시점을 고르게 한다.
- iOS Safari는 매니페스트를 쓰지 않으므로 `apple-touch-icon`과
  `apple-mobile-web-app-*` 메타를 따로 넣고, 공유 메뉴 안내를 띄운다.
- 아이콘은 `tools/`가 아니라 렌더링으로 생성한 PNG(192/512 + maskable 2종 + apple 180).

## 주의사항
- 법령·벌점 수치는 개정될 수 있으므로 "2026.3.9 시행 기준" 문구를 함께 노출.
- 공단이 학습용으로 공개한 자료지만, 외부 재배포 시 이용조건 확인 필요.
- 해설이 비어 있는 3문항(#418, #643, #964)은 UI에서 빈 칸이 보이지 않게 처리
  ("해설이 제공되지 않는 문항입니다" 대체 문구).
