# 배포 — Vercel

정적 사이트라 서버도, 환경변수도, 데이터베이스도 필요 없다.
빌드 결과물(`dist/`)만 올리면 끝이고 Vercel 무료 플랜으로 충분하다.

## 미리 알아둘 것

**앱 코드는 `main`에 있어야 한다.** Vercel은 저장소를 연결하면 기본적으로
`main`(프로덕션 브랜치)을 빌드한다. 작업 브랜치에만 코드가 있으면
`package.json`을 못 찾아 첫 배포가 실패한다.

## 순서

1. **코드를 `main`에 합친다** (PR 머지)
2. https://vercel.com 접속 → GitHub 계정으로 로그인
3. **Add New… → Project**
4. 목록에서 `myeonheo-road` 옆 **Import**
   - 목록에 없으면 *Adjust GitHub App Permissions*에서 저장소 접근을 허용한다
5. 설정 화면은 **그대로 두고 Deploy**
   - `vercel.json`에 프레임워크(Vite)·빌드 명령·출력 폴더가 이미 적혀 있다
6. 1~2분 뒤 `https://myeonheo-road.vercel.app` 같은 주소가 나온다

이후 `main`에 푸시할 때마다 자동으로 다시 배포된다.

## 폰 홈 화면에 추가

배포 주소는 HTTPS라 서비스워커가 동작한다(로컬 `localhost` 외에는 HTTPS가 필수다).

- **안드로이드 크롬** — 접속하면 "홈 화면에 추가" 배너가 뜬다. 또는 ⋮ → 앱 설치
- **아이폰 사파리** — 하단 공유 → 홈 화면에 추가
  (iOS는 배너를 띄울 방법이 없어 앱에서 이 경로를 안내한다)

## `vercel.json`이 하는 일

캐시 수명을 파일 성격에 맞게 지정한다. 잘못 두면 눈에 잘 안 띄는 문제가 생긴다.

| 경로 | 캐시 | 이유 |
|---|---|---|
| `/sw.js` | 캐시 안 함 | 서비스워커가 캐시되면 **새 버전이 영원히 안 내려간다** |
| `/manifest.webmanifest` | 캐시 안 함 | 아이콘·이름을 바꿨을 때 바로 반영되게 |
| `/assets/*` | 1년, immutable | 파일명에 해시가 붙어 내용이 바뀌면 경로도 바뀐다 |
| `/q/*` | 1년, immutable | 문항 카드는 문항번호로 고정된 정적 이미지다 |

`rewrites`는 없는 경로로 들어와도 앱이 뜨게 한다. 정적 파일이 먼저 검사되므로
카드 이미지나 `sw.js` 요청을 가로채지 않는다.

## 배포 후 확인

1. 주소를 열어 홈 화면이 뜨는지
2. 크롬 개발자도구 → Application → Manifest / Service Workers 에 등록됐는지
3. Network 탭에서 `Offline` 체크 후 새로고침 → 앱이 그대로 열리는지
4. 문항 이미지가 뜨는지(이미지형 필터로 확인)
