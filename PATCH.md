# 데이터 패치 v2 — 적용 필수

앞서 전달한 `bank.json` 에 두 가지 오류가 있었다. 이 패치의 `bank.json` 으로 **교체**할 것.

## 수정 1 — 이미지형 8문항이 실제로는 순수 문장형이었다
**#39, #262, #264, #405, #466, #509, #549, #651**

원인: 이 문항들은 보기 마커로 `①` 대신 변종 문자를 쓴다.
- #39, #466 → `ⓛ` (원본 PDF의 오타. circled small letter L)
- #509 등 → `➀➁➂➃` (다른 유니코드 계열)

기존 파서가 `①` 만 찾아서 보기 파싱에 실패했고, 그 결과 이미지형으로 잘못 분류됐다.
8문항 모두 그림·표가 전혀 없는 텍스트 문항이며, 보기 4개가 온전히 추출된다.

**효과**: 카드 이미지로 축소 렌더링되던 8문항이 텍스트로 표시되어 가독성 문제가 사라진다.

**할 일**: `public/q/` 에서 `39.jpg, 262.jpg, 264.jpg, 405.jpg, 466.jpg, 509.jpg, 549.jpg, 651.jpg`
8개 파일을 **삭제**한다(더 이상 참조되지 않음). 카드 293장이 남는다.

## 수정 2 — 5지선다 문항 수는 187이 아니라 185다
`nc` 를 5로 올릴 문항은 **185개**다. #39, #466 은 `ⓛ②③④` 로 4지선다이므로 여기 포함되지 않는다.
(이 두 문항의 오타 마커를 5번째 보기로 오인하면 187이 된다.)

원문자 변종 전체(`①➀⓵❶ⓛ` / `②➁⓶❷` / … / `⑤➄⓹❺`)를 포함해 재검사한 결과,
이미지형 293문항은 **5지선다 185 + 4지선다 108** 로 정확히 나뉜다. 누락·불명 0건.

`nc` 를 5로 잘못 설정하면 카드에 없는 ⑤ 버튼이 화면에 생겨 절대 정답이 될 수 없는
유령 보기가 표시된다. 반드시 `answer-key.csv` 의 값을 따를 것.

## 변경 후 확정 수치
| 항목 | 이전 | **수정 후** |
|---|---|---|
| 문장형 | 699 | **707** |
| 이미지형 | 301 | **293** |
| 5지선다(nc=5) | 187(오류) | **185** |
| 4지선다(nc=4) | — | **108** |
| 복수정답 | 282 | 282 (변동 없음) |
| 카드 파일 | 301장 | **293장** |

## 적용 절차
1. `src/data/bank.json` 을 이 패치의 파일로 교체
2. `public/q/` 에서 위 8개 jpg 삭제
3. `verify/` 폴더를 이 패치의 것으로 교체
4. `node verify/verify.mjs` 실행 → **실패 0건**이어야 한다
5. 앱에서 #39 와 #509 를 열어 텍스트 문항으로 정상 표시되는지 확인

## 유지할 것
`max(nc, 정답번호)` 안전장치는 좋은 방어 코드다. 데이터가 맞아도 그대로 두면 된다.
